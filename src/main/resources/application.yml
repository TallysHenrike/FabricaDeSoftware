server:
  port: ${port:80}
spring:
  datasource:
    password: 123456
    url: jdbc:postgresql://db:5432/eventos
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
