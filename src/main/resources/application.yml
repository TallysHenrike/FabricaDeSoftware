server:
  port: ${port:8080}
spring:
  datasource:
    password: 123456
    url: jdbc:postgresql://localhost:5432/eventos
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
